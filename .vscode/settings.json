{
    "cmake.configureOnOpen": true,

    "cmake.buildDirectory": "${workspaceFolder}/build",

    "cmake.runBuildDirectory": "${workspaceFolder}/build",

    "cmake.debugConfig": {
        "cwd": "${workspaceFolder}"
    }
}